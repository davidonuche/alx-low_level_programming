#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int dare;

	for (dare = 0; dare < 10; dare++)
	{
		printf("%d", dare);
	}
	printf("\n");
	return (0);
}
